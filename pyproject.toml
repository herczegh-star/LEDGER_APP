[project]
name = "ledger-app"
version = "0.1.0"
description = "Investiční portfolio tracker nad unified_format_raw"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.0",
    "flet>=0.25.0",
]

[project.scripts]
ledger-app = "main:main"
